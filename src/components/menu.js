import React from "react";

import "./menu.css";

export default function Menu(props) {
    return (
        <div className="sidebar">
            <button>New</button>
            <button>View</button>
        </div>
    );
}